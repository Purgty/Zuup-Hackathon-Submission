# Routing adapter: "mock" (default) | "mapbox" | "here"
ROUTING_ADAPTER=mock
MAPBOX_TOKEN=

# Notification adapter: "mock" (default) | "firebase"
NOTIFICATION_ADAPTER=mock
FIREBASE_SERVER_KEY=

# Server
PORT=4000
WS_PORT=4001

# Database
DATABASE_PATH=./data/zuup.db

NODE_ENV=development
